CREATE DATABASE student_management;

CREATE TABLE class(
	id int,
    name varchar(255)
);

CREATE TABLE teacher(
	id int,
    name varchar(255),
    age varchar(255),
    country varchar(255)
);

CREATE TABLE student(
	id int,
    name varchar(255),
    age varchar(255),
    country varchar(255)
);
